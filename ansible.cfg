[defaults]
nocows = true
retry_files_enabled = false
# Human-friendly YAML task output (default callback, ansible-core >= 2.13).
callback_result_format = yaml
# Match the upcoming ansible-core 2.24 default; facts are only available under
# the ansible_facts namespace, never as injected ansible_* top-level vars.
inject_facts_as_vars = false
